# Launch the RosettaR UI

Opens a Shiny application to interactively test Rosetta templates and
validation.

## Usage

``` r
run_rosetta_ui()
```

## Value

A character string containing the processed text (if out_template is
text), or a dataframe (if out_template is "df").
